<x-app-layout><div class="mx-auto max-w-3xl px-4 py-8"><p class="text-sm font-semibold text-rentara-teal">Ruang kerja aktif: {{ $workspace->name }}</p><h1 class="rentara-page-title mt-2">Detail tenant</h1>@if(session('status'))<p class="mt-4 rounded-lg border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800" role="status">{{ session('status') }}</p>@endif
<div class="rentara-card mt-6 p-6"><dl class="grid gap-4 sm:grid-cols-2"><div><dt class="text-sm text-slate-500">Nama</dt><dd class="font-semibold">{{ $tenant->name }}</dd></div><div><dt class="text-sm text-slate-500">Status</dt><dd>{{ $tenant->status->value === 'active' ? 'Aktif' : 'Nonaktif' }}</dd></div><div><dt class="text-sm text-slate-500">Telepon</dt><dd>{{ $tenant->phone ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Surel</dt><dd>{{ $tenant->email ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Unit</dt><dd>{{ $tenant->unit?->unit_number ?? 'Belum ditempatkan' }}</dd></div></dl><p class="mt-6 text-sm text-slate-600">Data identitas dan kontak darurat disimpan terenkripsi.</p><div class="mt-6 flex gap-3"><a href="{{ route('app.tenants.edit',$tenant) }}" class="rounded-lg bg-rentara-blue px-4 py-2.5 font-semibold text-white">Ubah</a><a href="{{ route('app.tenants.index') }}" class="rounded-lg border px-4 py-2.5">Kembali</a></div></div></div></x-app-layout>
