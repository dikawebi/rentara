<x-app-layout><div class="mx-auto max-w-4xl px-4 py-8 sm:px-6 lg:px-8"><p class="text-sm font-semibold text-rentara-teal">Ruang kerja aktif: {{ $workspace->name }}</p><h1 class="rentara-page-title mt-2">Anggota ruang kerja</h1><section class="rentara-card mt-6"><div class="divide-y divide-slate-100">@forelse($workspace->members as $member)<div class="flex items-center justify-between gap-4 py-4"><div><p class="font-semibold text-rentara-navy">{{ $member->user->name }}</p><p class="text-sm text-slate-500">{{ $member->user->email }}</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{{ $member->role->value }} · {{ $member->status->value }}</span></div>@empty<p class="py-4 text-sm text-slate-600">Belum ada anggota untuk ditampilkan.</p>@endforelse</div></section></div></x-app-layout>
