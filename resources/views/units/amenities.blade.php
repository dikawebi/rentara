<x-app-layout><div class="mx-auto max-w-2xl px-4 py-8 sm:px-6 lg:px-8"><a class="text-sm text-rentara-blue" href="{{ route('app.properties.units.index', $property) }}">Kembali ke unit</a><h1 class="mt-2 rentara-page-title">Fasilitas {{ $unit->unit_number }}</h1><form class="rentara-card mt-6 space-y-4 p-6" method="POST" action="{{ route('app.properties.units.amenities.update', [$property, $unit]) }}">@csrf @method('PUT') @foreach($amenities as $amenity)<label class="flex gap-3"><input type="checkbox" name="amenity_ids[]" value="{{ $amenity->id }}" @checked($unit->amenities->contains($amenity->id))> <span>{{ $amenity->name }}</span></label>@endforeach @if($amenities->isEmpty())<p>Belum ada fasilitas pada ruang kerja.</p>@endif<button class="rounded-lg bg-rentara-blue px-4 py-2.5 text-sm font-semibold text-white">Simpan fasilitas</button></form></div></x-app-layout>
