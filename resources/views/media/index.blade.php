<x-app-layout><div class="mx-auto max-w-4xl px-4 py-8 sm:px-6 lg:px-8"><h1 class="rentara-page-title">Media {{ $unitModel ? 'unit '.$unitModel->unit_number : $propertyModel->name }}</h1>@if(session('status'))<p class="mt-4 text-sm text-green-800">{{ session('status') }}</p>@endif<form class="rentara-card mt-6 space-y-4 p-6" method="POST" enctype="multipart/form-data" action="{{ route('app.properties.media.store', [$propertyModel, $unitModel?->id]) }}">@csrf<input type="file" name="file" accept="image/jpeg,image/png,image/webp" required><input class="block w-full rounded-lg border-slate-300" name="caption" placeholder="Keterangan"><button class="rounded-lg bg-rentara-blue px-4 py-2.5 text-sm font-semibold text-white">Unggah</button></form><div class="mt-6 grid gap-4 sm:grid-cols-2">@foreach($media as $item)<article class="rentara-card p-4"><img class="h-48 w-full object-cover" src="{{ route('app.media.stream', $item) }}" alt="{{ $item->caption ?? $item->original_name }}"><p class="mt-2 text-sm">{{ $item->caption ?? $item->original_name }}</p><form class="mt-2" method="POST" action="{{ route('app.media.destroy', $item) }}">@csrf @method('DELETE')<button class="text-sm font-semibold text-red-700">Hapus</button></form></article>@endforeach</div></div></x-app-layout>
